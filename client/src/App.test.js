import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

// delete the test for now since the main app
// does a lot of API calls. can fake it out later
test('renders learn react link', () => {});
